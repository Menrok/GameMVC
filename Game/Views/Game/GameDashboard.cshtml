@model Game.Models.Hero

<div class="container">
    <h2 class="text-center my-4">Witaj, @Model.Name!</h2>

    <div class="d-flex flex-column align-items-center">
        <a href="@Url.Action("Missions", "Game", new { heroId = Model.Id })" class="btn btn-primary mb-2">Misje</a>
        <a href="@Url.Action("Hero", "Game", new { heroId = Model.Id })" class="btn btn-primary mb-2">Bohater</a>
        <a href="@Url.Action("Shop", "Game", new { heroId = Model.Id })" class="btn btn-primary mb-2">Sklep</a>
        <a href="@Url.Action("Fight", "Game", new { heroId = Model.Id })" class="btn btn-primary mb-2">Wyprawa</a>
    </div>
</div>